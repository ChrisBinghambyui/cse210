"""
def get_space(board):
    want to change = true
    while want to change = true:
        GET target (like b5)
        assert target is valid (not zz or 59)
        assert IS empty (=0)
        if target is empty:
            return target
        if not empty:
            print whats in the spot
            get want to change?
"""